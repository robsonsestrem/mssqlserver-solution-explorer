---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
-- Agora muita atenção na ordem dos passos, vamos utilizar duas sessões no mesmo banco, sessão 1 e sessão 2.
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
-- Tabela de teste
CREATE TABLE [tb_TesteLock]
([id]          INT,
 [produto]     VARCHAR(255),
 [imobiliaria] INT,
 [faixa_preco] INT,
 [observacao]  VARCHAR(max)
);

INSERT INTO [tb_TesteLock] VALUES (1, 'a', 1, 1, 'b')

---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

-- sessão 01
BEGIN TRANSACTION; 

UPDATE [tb_TesteLock]
  SET
      [observacao] = 'C';

---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

-- sessão 02
BEGIN TRANSACTION; 

UPDATE [tb_TesteLock]
  SET
      [observacao] = 'D';

-- Após 10 segundos já vai ficar nos registros